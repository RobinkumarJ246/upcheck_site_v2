// Server component for metadata
export const metadata = {
  title: 'Survey | UpCheck',
  description: 'Participate in our survey and share your valuable feedback.',
};

export default function SurveyLayout({ children }) {
  return children;
}
